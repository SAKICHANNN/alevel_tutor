# W4 Log — Grading Calibration & Infrastructure

> 日期：2026-06-01  
> 状态：全部完成  
> Commits: dbc4220, 0200245, b7981ba  

---

## §0 原计划 vs 实际偏离

### 原计划（development_plan.md v3）

development_plan.md 写于 v0.1.0（commit 7873ba0），当时项目刚搭完骨架还未端到端验证。W4 原计划是：

| 原计划 | 状态 |
|--------|------|
| W4.1 批改校准 vs 真人评分（MAE<0.5, r>0.85） | 完成了，但方法完全不同（见 §7 偏离） |
| W4.2 SQLite 数据模型 6 表 | 按计划完成 |
| W4.3 error_logs 自动记录 | 按计划完成 |
| W4.4 对话持久化 | 按计划完成 |
| W4.5 成本日志 + BudgetGuard | 按计划完成 |
| W4.6 ChromaDB 持久化策略 | 按计划完成 |
| W4.8 Reranker 模型下载确认 + 生产接入 | 提前完成（模型已在 W3 下载，W4 只需开关） |
| Gradio Web 前端 | **未做** — 推迟到 W5 |
| Docker Compose 打包 | **未做** — 推迟到 W5-6 |

### 关键偏离

**偏离 1：评分方法彻底重构**

原计划 W4.1 是「用已有的 grading JSON prompt 跑校准、对比真人评分」。实际上：
- 原 grading JSON prompt 有严重缺陷：tools 干扰 JSON-only 指令、max_marks 无约束、无真实 mark scheme 匹配
- 校准过程中做了深度网络搜索，发现 PedCoT（IJCAI 2024）是 SOTA 数学错误检测方案
- 最终重写了整个评分引擎：`agent/grading/grader.py`（原 W1 的 `core.py:grade()` 改为委托调用）
- 原评分模块是单个 prompt，现在是 PedCoT 两阶段 + 真实 MS 匹配 + M-A-B 评分三元路径

**偏离 2：执行顺序调整**

原计划 W4.2→W4.3→W4.4→W4.5→W4.8→W4.6→W4.1，实际合并了 W4.2-W4.5 为一次提交（dbc4220），因为它们都在 `agent/database.py` 和 `agent/tutoring/core.py` 中耦合实现。

**偏离 3：Reranker 已是 W3 完成的**

原计划 W4.8 是「模型下载状态确认 + 接入生产检索」。实际上 W3 已下载到 ModelScope 缓存，W4.8 只在 core.py 中改两个 `use_rerank=True` 就完成了。原计划对模型下载的困难估计不足（以为需要翻墙等），W3 已经解决。

**偏离 4：真人评分对照未完成**

原计划要求「真人评分对照」，但我们没有真实学生数据。改为：合成 gold set（15 案例，含完美/部分/错误/无过程各质量等级），基于 Cambridge 官方 M-A-B 规则人工设定 gold scores。这是 pilot 阶段最务实的方案。

---

## §1 W4.2 SQLite 数据模型

创建 `agent/database.py` — 6 表 + BudgetGuard：

| 表 | 用途 |
|---|------|
| users | 单用户 (pilot desktop app) |
| conversations | 对话会话，含 subject_code, title |
| messages | 角色消息 + tool_calls/citation_chips JSON |
| grading_results | 结构化评分 + rubric 四维度 |
| error_logs | 错误类型/来源/消息/标签/堆栈 |
| cost_logs | token 数 + 费用（USD+CNY） |

Schema 包含：WAL mode, busy_timeout=5000, foreign_keys=ON。
Indexes: messages.conversation_id, grading_results.conversation_id, error_logs.created_at, cost_logs.created_at, cost_logs(model_key, created_at)。

定价表（USD per 1M tokens, 2026-06 最新）：
- deepseek-v4-flash: $0.14 prompt / $0.28 completion
- glm-4v-plus: $0.72 / $0.72
- qwen-vl-max: $0.42 / $1.26
- qwen/qwen3-vl-8b (LM Studio): $0/$0 — 本地免费
- USD/CNY = 7.25

设计决策：
- 不用 ORM（SQLAlchemy 对 pilot 过度工程化）
- 不用 async（CLI 单线程）
- 所有写入通过线程锁 `threading.Lock()`
- TEXT 时间戳（SQLite 无原生 datetime）
- JSON 字段存为 TEXT

---

## §2 W4.3 Error Logs 自动记录

集成到 `Agent._call_llm()` — 每次 API/工具/解析/视觉失败自动写入 error_logs。

已验证的错误类型（8 种）：
| error_type | 触发场景 |
|------------|---------|
| api_call | HTTP 4xx/5xx, 网络超时 |
| grading_fail | PedCoT Stage 1/2 失败 |
| tool_fail | search_textbook 等工具异常 |
| parse_fail | grading JSON 解析失败 |
| timeout | API 调用超时（120s） |
| vision_fail | LM Studio/Qwen VLM 失败 |
| embed_fail | Ollama embedding 失败 |
| unknown | 无法归类的异常 |

已验证的 misconception_tags（8 种）：concept, method, algebra, units, diagram_reading, essay_structure, translation, carelessness, system_error。

记录内容：conversation_id, subject_code, error_message（截断 200 字符）, stack_trace（截断 500 字符）, payload_snippet（截断 500 字符）。

---

## §3 W4.4 对话持久化

Agent 新增 `conv_id: int` 参数。chat.py 启动时 `create_conversation(title="CLI Session")` → 传给 Agent → 每条消息 `save_message()`。

消息字段：conversation_id, role (user/assistant/system/tool), content, tool_calls (JSON), tool_call_id, citation_chips (JSON), token_count, created_at。

Agent.chat() 中的持久化点：
- 用户消息：`save_message(conv_id, "user", user_input)`
- 助手回复（含工具调用）：`save_message(conv_id, "assistant", content, tool_calls=...)`
- 工具结果：`save_message(conv_id, "tool", result, tool_call_id=...)`
- 错误消息：同上
- 图片批改结果：同上

Agent.reset() 时创建新 conversation（而非清除旧数据 — 数据不会丢失）。

---

## §4 W4.5 Cost Logging + BudgetGuard

### Cost Logging

`_call_llm()` 从 API response 的 `usage` 字段提取：
- prompt_tokens
- completion_tokens
- reasoning_tokens（从 `completion_tokens_details.reasoning_tokens`）
- total_tokens
- latency_ms（从请求开始到结束）

费用计算：prompt_tokens / 1M * prompt_price + completion_tokens / 1M * completion_price。
USD → CNY 汇率 7.25。

### BudgetGuard

默认预算（可通过 env var 覆盖）：
- `BUDGET_MONTHLY_CNY=50`：月度 ¥50
- `BUDGET_DAILY_CNY=2`：每日 ¥2

超限策略：抛出 RuntimeError("预算限制: ...")，阻止后续 API 调用。
check_budget() 在每次 `_call_llm()` 前执行。

### CLI 命令

`/cost` 显示：
- 累计调用次数
- 累计 tokens
- 累计费用（¥ + $）
- 预算状态（正常/超限）
- 最近 7 天每日明细

`/stats` 显示：知识库统计 + 简要成本信息。

### 开发期费用

W4 开发测试实际消耗：~30 次 API 调用，~¥0.05。
成本极低因为：PedCoT 校准只有 15 个案例（每个 1-2 次调用），其余都是本地测试。

---

## §5 W4.8 Reranker 生产接入

Agent 默认 `use_rerank=True`。两处搜索引擎入口：

```python
# core.py _execute_tool()
search_textbooks(..., use_rerank=True)
search_past_papers(..., use_rerank=True)
```

状态：
- 模型已下载到 ModelScope 缓存（W3）
- 加载时间：1-3s（lazy load，首次搜索触发）
- Rerank 延迟：~1s/10 对（CPU）
- 可用性确认：`is_available()` → True
- 降级策略：若 reranker 不可用，返回原始 top-N 结果

原计划 W4.8 在 dev plan 中标记为「模型首次下载被 GFW 阻塞 — 需在有网络的机器上运行」。W3 已通过 ModelScope 绕过，W4 只需改两行代码。

---

## §6 W4.6 ChromaDB 备份/恢复

创建两个工具脚本（`tools/scripts/`）：

### backup_chroma.py
- 时间戳目录：`data/backups/chroma_YYYYMMDD_HHMMSS/`
- 备份前 WAL checkpoint：`PRAGMA wal_checkpoint(TRUNCATE)` — 确保 WAL 日志归入主 DB
- 复制：ChromaDB 686 MB + tutor.db 60 KB
- Manifest JSON：时间戳、大小、创建时间
- 自动清理：`--keep 7` 保留最新 7 个备份
- 可选：`--no-verify` 跳过 ChromaDB 完整性检查
- 可选：`--path` 自定义备份路径

### restore_chroma.py
- 安全备份：恢复前自动创建 `data/backups/pre_restore_auto/` 保存当前数据
- 验证：检查 chroma.sqlite3 是否存在且 collections > 0
- 恢复流程：删除当前 ChromaDB → 复制备份 → 恢复 tutor.db
- 支持：`--list` 列出所有备份、`--latest` 恢复最新、`--dry-run` 预览

### 未做的

原计划要求「每日自动备份脚本」和「恢复脚本在 W4 报告中验证」。
- 每日备份：未集成到 cron/launchd，需要手动运行，或加到 chat.py 启动时可选执行
- 恢复验证：只做了 list + manifest 检查，未实际恢复后跑测试（恢复会覆盖当前 ChromaDB，有风险）

---

## §7 W4.1 评分校准 — 偏离原方案，重新设计

### 7.0 原方案与失败原因

**原方案**：用已有的 `GRADING_JSON_PROMPT`（`agent/tutoring/prompts.py`），准备 15 道题 + gold scores，跑 grade() 对比。

**第一次尝试**：agent.grade() 返回 `JSONDecodeError: Expecting value: line 1 column 1 (char 0)`。
- 原因 1：`_call_llm()` 默认传入 `tools=TOOLS`，干扰 "Output valid JSON only" 指令，LLM 返回空 content
- 原因 2：`tools_list: list = None` 默认值时 `tools_list if tools_list is not None else TOOLS` = TOOLS，即「显式传 None = 用全部工具」，逻辑反了
- fix 1：改为 `tools_list: list = TOOLS`，显式传 `[]` 才无工具（commit dbc4220）

**第二次尝试**：agent.grade() 返回 `score_awarded: 10, score_max: 10`（题目满分只有 3 分）。
- 原因：prompt 中 `"score_max": <number>` 无上限约束，LLM 自己猜满分
- fix 2：在 prompt 中解析 mark scheme 的 `[N]` 标注推算满分，插入 `{max_marks}`（commit dbc4220）
- 结果：MAE=0.167, r=0.961 — PASS，但用的是合成 mark scheme，非真实材料

**第三次尝试（用户要求）**：必须用真实 Cambridge mark scheme 对照。
- 尝试 1：搜索 ChromaDB QP chunks → 内容是省略号（PyMuPDF text 提取失败，2022 试卷文本全是 `...`）
- 尝试 2：用 LM Studio VLM 逐页渲染 MS PDF → 成功提取 Q2 + 对应 MS，但每页 20-30s，19 页 MS 全扫需 10 分钟
- 尝试 3：搜索 ChromaDB MS chunks → 内容有编码损坏（``=`-`, ``=`=`, ``=`∫`）但可读
- 结论：当前 PDF 文本提取不行，需要真正的 QP+MS 对照数据集，或者用 VLM 批量提取

**此时用户提出更深层的问题**：当一道初见的题匹配不到完全对应 MS 时，如何确保仍得到严谨批改？

### 7.1 深度研究（4 轮 web search）

进行了系统性的文献研究，覆盖：

**Cambridge 官方评分体系**（来源：官网 MS PDF，papacambridge, studocu, excelas.ai）
- M-A-B 三分法：Method（方法）/ Accuracy（精确）/ Bonus（独立）
- M→A 依赖：A 分必须在 M 分获得后才能给
- FT（Follow-Through）：前序结果错但后续方法对，照给 M 分
- 正向评分：只加分不扣分，只给整数分，错写不罚
- ISW（Ignore Subsequent Working）：正确后的错误忽略
- 3 s.f. 标准，角度到 1 d.p.
- 关键发现：M 分不因算术错误扣分，但必须「把方法应用于具体问题」，只引用公式不够

**自动化评分研究**：

| 论文 | 方法 | 结果 | 相关性 |
|------|------|------|--------|
| MathMistake Checker (arXiv 2503.04291) | OCR + LLM PedCoT 两阶段 | 无参考答案的开放式评分 | 直接可借鉴 |
| PedCoT (Jiang et al. IJCAI 2024) | 两阶段：Regenerate → Extract-Compare | MF Acc 83% vs baseline 74% | **核心参考** |
| ProofGrader (arXiv 2510.13888) | LLM + 参考方案 + MS 作为 context | MAE 0.926/7 | MS 质量很关键 |
| GPT-5 Calculus (arXiv 2510.05162) | 置信度过滤 + IRT 风险 | R²=0.95（严格过滤后） | 置信度过滤思路 |
| Pensieve Grader (arXiv 2507.01431) | 端到端：转录→评分→置信度→反馈 | 65% 时间节省 | 全流程参考 |
| Rubric Injection (D19-6119) | 将 rubric key elements 注入 SAG | 低资源下显著提升 | rubric 生成思路 |

**关键发现：PedCoT 两阶段必要性**

Jiang et al. 做了 ablation：单阶段 vs 双阶段。
- 单阶段：LLM 同时看到问题和学生答案 → conformality bias（LLM 偷懒，倾向于同意学生已有答案），P+ 从 81.25% 掉到 53.33%
- 双阶段：Stage 1 LLM 不看学生答案独立解题 → Stage 2 对照评分 → P+ 81.25%, R- 96.22%
- **结论：必须两阶段**。先让 LLM 自己解一遍，再对照。这个发现直接指导了我们的评分重构。

### 7.2 实现：PedCoT 三路径评分引擎

创建 `agent/grading/grader.py` — 替换原来的 `agent/tutoring/prompts.py:GRADING_JSON_PROMPT`。

**路径 1：真实 MS 匹配**
```
question → search_past_papers(ms, rerank=True) → if score > 0.75
  → 直接用真实 MS 评分（1 次 API 调用）
```
搜索 ChromaDB past_papers collection (paper_type="ms")，Reranker 阈值 0.75。

**路径 2：PedCoT 两阶段**
```
if 无匹配 MS:
  Stage 1 (Regenerate): LLM 只看到 question → 生成:
    [概念] [方法步骤] [计算过程] [M-A-B 分配表]
  
  Stage 2 (Extract-Compare-Grade): LLM 看到 question + Stage-1 输出 + student_answer → 
    → 提取学生实际步骤
    → 逐步与 Stage-1 预期对照
    → 按 M-A-B 规则打分
    → 输出 JSON {score_awarded, mark_points[], rubric, ...}
```

**路径 3：降级**（Stage 1 或 Stage 2 失败 → 返回 fallback `score=0, tags=["system_error"]`）

**PedCoT Stage 1 Prompt 设计**（基于 Bloom 认知模型的三个层次）：
1. **Remember（记忆）**：列出涉及的数学概念
2. **Understand（理解）**：分解解决方法步骤
3. **Apply（应用）**：执行计算并分配 M-A-B 分

**PedCoT Stage 2 Prompt 设计**：
- 包含完整的 Cambridge 评分规则（M/A/B/FT/DM/ISW/3sf/正向评分）
- 逐 mark point 判断：student earned this mark? why/why not?
- 应用 M→A 依赖：M 未得则 A 不得
- 应用 FT：前序错但方法对 → M 分照给
- 输出 JSON 含 mark_points[] (逐分说明)

### 7.3 校准结果

**数据**：15 个案例，4 个 topic（P1 × 6, Integration × 3, Differentiation × 3, Mechanics × 3），涵盖 4 种质量等级（perfect/partial/wrong/no-working）。

**结果**：
- **MAE = 0.300 分** (< 0.5 ✓ PASS)
- **Pearson r = 0.898** (> 0.85 ✓ PASS)

**逐案例分析**：

| ID | Gold | Agent | Δ | 分析 |
|----|------|-------|---|------|
| P1-01 | 2/2 | 2/2 | 0.0 | 完美匹配 |
| P1-02 | 1/2 | 1/2 | 0.0 | 方法对计算错→M1 得 A1 失，正确 |
| P1-03 | 0/2 | 0/2 | 0.0 | 只有错误答案无过程→0 分，正确 |
| P1-04 | 3/3 | 2/2 | +1.0 | Stage-1 自动给满分 2，Cambridge 给 3。自动 MS 低估了满分 |
| P1-05 | 3/3 | 3/3 | 0.0 | 完美匹配 |
| P1-06 | 1.5/3 | 1/2 | +0.5 | 接近，分数尺度差异 |
| INT-01 | 4/4 | 4/4 | 0.0 | 完美匹配 |
| INT-02 | 3/4 | 3/4 | 0.0 | 忘记 +C→扣 1 分，正确 |
| INT-03 | 0/4 | 1/4 | -1.0 | 代理偏宽松，对有尝试但全错给了 1 分 |
| DIF-01 | 3/3 | 3/3 | 0.0 | 完美匹配 |
| DIF-02 | 3/3 | 3/3 | 0.0 | 完美匹配（fix 后不再 hallucinate 10/10） |
| DIF-03 | 1.5/3 | 0/3 | +1.5 | 代理偏严。学生写了 5(2x+1)^4 但少了乘 2，给了 0。应为 1-2 分 |
| MEC-01 | 2/2 | 2/2 | 0.0 | 完美匹配 |
| MEC-02 | 0/2 | 0/2 | 0.0 | 全错→0 分，正确 |
| MEC-03 | 1.5/2 | 2/2 | -0.5 | 代理偏宽松。极简答案 "a=6t-4=8" 给了满分，应为部分分 |

**偏差模式总结**：

1. **自动 MS 满分偏差**（P1-04）：Stage-1 自动生成的满分比 Cambridge 官方少 1 分。自动 MS 合并了某些 mark points（如 A1+A1 合并为单个 A1）。
2. **代理偏严**（DIF-03）：当学生答案缺少中间步骤时，代理倾向于 0 分而非部分分。Cambridge 考官会给 partial credit 如果能看到部分正确方法。
3. **代理偏松**（INT-03, MEC-03）：当学生有尝试意图但结果不对时，代理倾向给同情分。Cambridge 考官更严格。
4. **10/15 完全匹配**：在答案明确对错（有完整过程且结果对、有完整过程且结果错、无过程且答案错）的案例中代理表现优秀。

### 7.4 限制和后续

**当前限制**：
- 自动 MS 满分可能与 Cambridge 官方不一致（±1 分），需要更大的 MS 匹配库
- MS 匹配率不够高：ChromaDB 中 MS chunks 有编码损坏（``, ``），影响匹配质量
- QP 文本提取失败（2022 试卷全是省略号），无法做 QP→MS 对照
- 真人评分对照缺失：gold set 是人工基于 Cambridge 规则设定的，不是真人评分
- 15 个案例不够有代表性：需要扩展到 50+ 案例，涵盖更多 topic、更多题型

**建议后续（W5+）**：
1. 用 VLM 批量提取 QP+MS 对照集（离线批处理，每页 20-30s，可忍受）
2. 建立 question→MS 直接映射表（200+ 题），超过 Reranker 匹配
3. 收集或模拟真人学生答案（错误类型多样化），找有 A-Level 经验的真人评分做对照
4. 对 PedCoT Stage-1 的输出做质量检查（MS 满分是否合理）
5. 引入置信度阈值 deferral（如 ProofGrader/GPT-5 论文中，低置信度路由到人工）

---

## §8 W4 Bug Fixes

### Bug 1: _call_llm tools_list 默认值反逻辑
**症状**：grade() 调用 `_call_llm(messages, tools_list=None)` 期望无工具，但 `tools_list if tools_list is not None else TOOLS` = TOOLS。导致 LLM 看到工具定义 + "JSON only" 指令，返回空 content。
**修复**：默认改为 `tools_list: list = TOOLS`，显式传 `[]` 才无工具。
**影响范围**：仅 grade() 受影响（它是唯一需要无工具调用的路径）。

### Bug 2: grading max_marks 无约束 → 分数 hallucination
**症状**：DIF-02 返回 `score_awarded: 10, score_max: 10`（实际满分 3）。
**修复**：在 GRADING_JSON_PROMPT 中插入 `{max_marks}`，从 mark scheme 的 `[N]` 标注推算。
**验证**：重新跑校准，DIF-02 从 10/10 恢复到 3/3。

### Bug 3: calibration 输出路径错误
**症状**：`Path(__file__).parent.parent.parent / "data" / "eval"` 计算为 `/Users/miaomiao/data/eval/` 而非项目内。
**修复**：改用 `Path(__file__).parent.parent`（tests/ → project root）。
**教训**：`__file__` 相对路径在测试文件中不可靠。

### Bug 4: do_reset 被 do_cost 覆盖
**症状**：编辑 chat.py 时 `do_cost` 替换匹配到了 `do_reset` 的 `def do_reset` 行，导致 `do_reset` 方法消失且被重复。
**修复**：手动恢复并去重。
**教训**：`edit_file` 的 oldString 匹配无上下文时容易误匹配。应提供更多周围代码。

---

## §9 做完的事 vs 故意没做的事

### 做完的
- W4.2-W4.6, W4.8 全部完成
- W4.1 评分校准完成（MAE=0.300, r=0.898）
- Bug fixes: 4 个
- 深度研究：6 轮 web search，覆盖 Cambridge 官方 MS + 6 篇论文
- PedCoT 评分引擎：从 0 实现（`agent/grading/grader.py`）
- 备份/恢复工具
- .gitignore 更新

### 故意没做的
- **Gradio Web 前端**：推迟到 W5。CLI 功能完整，Web 需要额外工程（图片上传、流式输出、前端状态管理）
- **Docker Compose**：推迟到 W5-6。需要先稳定所有依赖（Ollama, LM Studio, ChromaDB 路径映射）
- **真人评分对照**：无真实数据。需要联系有 A-Level 教学经验的老师或收集公开的学生答案集
- **每日自动备份**：未集成 cron/launchd。手动备份可工作，自动化留到部署阶段
- **4b embedding 升级**：W3 尝试失败（silent crash），保持 0.6b 稳定。4b 路径通过 env var 保留
- **多科目评分校准**：当前只做了 9709 Mathematics。需要在 W5 扩展到 9701/9702/9708

---

## §10 统计数据

- 新增文件：7 (agent/database.py, agent/grading/grader.py, agent/grading/__init__.py, backup_chroma.py, restore_chroma.py, test_calibration.py, calibration_report.json)
- 修改文件：4 (core.py, prompts.py, chat.py, .gitignore)
- 新增代码：~1,388 行
- 删除代码：~40 行（旧 grading prompt 被 PedCoT 替代）
- API 调用（开发测试）：~40 次（~¥0.07）
- 数据库记录：messages 18, grading_results 17, cost_logs 42, error_logs 8
- 备份：1 个副本，686 MB
- Web search：6 轮（1 轮 deep + 5 轮 targeted）
- 论文阅读：6 篇（PedCoT, MathMistake Checker, ProofGrader, GPT-5 Calculus, Pensieve Grader, Rubric Injection）
