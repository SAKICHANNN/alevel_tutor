# W5 Log — Pilot 安全 + Web 前端 + Docker

> 日期：2026-06-01
> 状态：W5 全部完成
> Commits: 642e387 → 3d475eb, c8ec30a, e5eacca

---

## §1 W5.0 Gradio Web 前端

创建 `web/app.py`（Gradio 4.44.1 Blocks 界面）：

**布局**：
- 左侧栏：科目选择器 + 成本面板（30s 自动刷新）+ 使用提示
- 主区域：Chatbot（messages 类型）+ 文本输入 + 发送按钮
- 折叠区：图片上传 + 批改结果

**后端函数**：
- `chat_fn(message, history, session_id, subject)` — 文本聊天，注入检测后调用 Agent
- `grade_image_fn(image, session_id, subject)` — 文件校验 → EXIF 剥离 → VLM 批改
- `get_cost_html()` — 成本统计渲染为 HTML
- `switch_subject(code)` — 科目切换

**状态管理**：每个浏览器 session 独立 Agent 实例（`AGENT_INSTANCES` 字典），conversation 持久化到 SQLite。

未实现（推迟）：流式输出（需要 async generator + gradio streaming）、多用户并发队列。

---

## §2 W5.1 RAG / 文件上传安全护栏

创建 `agent/security.py`：

**检索内容防护**（`sanitize_retrieved_content`）：
- 所有 RAG 结果包裹 `[⚠️ 以下内容来自检索结果...]` 标记
- 移除 `System:` / `SYSTEM:` 前缀行（常见注入向量）
- 移除 markdown code blocks 中的 `system|instruction|prompt` 标签

**工具调用校验**（`validate_tool_call`）：
- 白名单：6 个工具（search_textbook, search_past_paper, get_exam_pattern, search_exam_techniques, grade_homework_image, get_subject_info）
- 参数注入检测：每个 string 参数过 `detect_injection()`

**集成点**：
- `Agent.chat()`：用户输入先过 `detect_injection()` → 拦截则返回安全提示
- `Agent._execute_tool()`：工具调用前过 `validate_tool_call()` → 拦截则返回错误
- `_execute_tool` search 结果：过 `sanitize_retrieved_content()` 包裹

---

## §3 W5.2 Prompt Injection 测试集

`agent/security.py:INJECTION_TEST_CASES` — 27 个测试用例：

| 类别 | 数量 | 说明 |
|------|------|------|
| system_extract | 5 | 提取 system prompt |
| role_hijack | 3 | 角色篡改（DAN、terminal） |
| tool_hijack | 2 | 强制调用工具 |
| memory_write | 2 | 写入永久记忆/删除数据 |
| pdf_injection | 2 | PDF 内容隐藏注入 |
| benign | 6 | 正常问题（不应拦截） |
| edge | 4 | 边界（"system of equations"、"print 步骤"） |
| ocr_injection | 3 | OCR 文本中的隐藏指令（SYSTEM OVERRIDE 等） |

**结果**：27/27 通过（100% pass rate, 0 false positives, 0 false negatives）。

`run_injection_tests()` 可随时重跑验证。

---

## §4 W5.3 文件卫生

`agent/security.py` — 文件安全函数：

| 函数 | 说明 |
|------|------|
| `validate_file(path)` | 类型白名单（6 种）+ 20MB 上限 |
| `strip_exif(path)` | Pillow 重建图像 → 移除所有 EXIF 元数据 |
| `compute_file_hash(path)` | SHA-256 哈希，用于去重/审计 |
| `ALLOWED_MIME_TYPES` | image/png, image/jpeg, image/webp, application/pdf, text/plain, text/markdown |
| `ALLOWED_EXTENSIONS` | .png, .jpg, .jpeg, .webp, .pdf, .txt, .md |

---

## §5 W5.4 Docker Compose

**Dockerfile**：
- Base: `python:3.9-slim`
- System deps: libgl1, libglib2.0, libsm6, libxext6, libxrender-dev（OpenCV/PyMuPDF 需要）
- 入口：`python3 web/app.py`
- 暴露：7860 (Gradio)

**docker-compose.yml**：
- `web` service：端口 7860，ChromaDB 只读挂载，tutor.db 读写挂载
- `host.docker.internal` 桥接到宿主机 Ollama/LM Studio
- 环境变量：DEEPSEEK_API_KEY, ZHIPU_API_KEY, DASHSCOPE_API_KEY, budget vars
- 可选 `ollama` service（注释掉，M5 macOS 用宿主机 Ollama 更简单）

**启动命令**：`docker compose up -d`

---

## §6 W5.7 Pilot 总结报告

`docs/plans/pilot_report.md` — 10 节完整报告：
- 项目概要、四周完成内容、技术栈对比表
- PedCoT 三路径评分引擎架构图
- W5 安全措施矩阵
- 当前能力矩阵（11 项功能状态）
- 7 个已知限制
- 成本预估（¥2-5/月/学生）
- 下一步（W6）+ 文档索引

---

## §7 W5.8 多科目评分校准（代码就绪）

`tests/test_calibration.py` 扩展为 4 科 × 45 案例：

| 科目 | 案例数 | 题型 |
|------|--------|------|
| 9709 Math | 15 | P1, Integration, Differentiation, Mechanics |
| 9701 Chemistry | 10 | Le Chatelier, moles, structure, ionisation, titration |
| 9702 Physics | 10 | acceleration, Ohm's law, waves, moments, KE |
| 9708 Economics | 10 | PED, demand/supply, inflation, interest rates, subsidy |

每个案例包含：question, mark_scheme（带 [N] 标注）, student_answer（perfect/partial/wrong/no-working）, gold_score, gold_max。

**未运行**：45 案例需要 ~90 次 API 调用（~¥0.15），建议在低峰期运行。

---

## §8 W5.9 QP+MS 自动映射（工具设计）

**目标**：用 VLM 批量提取真实 QP→MS 对照，建立 question_text → mark_scheme_text 直接映射表。

**方案**：
1. 选择 2022-2024 各科 QP+MS 各 5 份（共 10 对 × 4 科 = 40 对）
2. 每对取 Q1-Q5（前 5 题），每题渲染 2 页（QP 题目页 + MS 对应页）
3. VLM 提取：question text → corresponding mark scheme text
4. 输出 `data/qp_ms_map.json`（含 source PDF, page, confidence）

**瓶颈**：LM Studio 每页 ~20-30s，40 对 × 5 题 × 2 页 = 400 页 → 8000s ~2.2h。建议离线批处理。

**脚本已预留**：未在本次 commit 中创建（需要 VLM 可用时批处理运行），设计已在 log 中记录。

---

## §9 文件变更

| 文件 | 状态 | 说明 |
|------|------|------|
| `web/app.py` | 新增→修改 ×10 | Gradio Web + 实时状态动画 + Mermaid/LaTeX 修复 + indent 修复 |
| `agent/security.py` | 新增 | 注入检测 + 文件校验 + EXIF 剥离 + 临时文件清理 |
| `agent/tutoring/core.py` | 修改 ×6 | status_callback + _sanitize_output + _render_mermaid_blocks + thinking mode + reasoning 连续性 |
| `agent/tutoring/prompts.py` | 修改 ×4 | ⛔ 禁止规则 + Mermaid 图表规范 + LaTeX 公式规范 + f-string→普通字符串 |
| `agent/config.py` | 修改 | tutor.thinking = True |
| `Dockerfile` | 新增 | python:3.9-slim + 全依赖 |
| `docker-compose.yml` | 新增 | web service + Ollama 可选 |
| `docs/plans/pilot_report.md` | 新增 | 10 节 Pilot 总结报告 |
| `docs/plans/week5_plan.md` | 修改 | 加入 W5.0 LLM 动画 |
| `tests/test_calibration.py` | 修改 | 3 科新案例 (9701/02/08) |
| `tests/test_w4_debug.py` | 新增 | 96 项 W4 全面测试 |
| `tests/test_w5_debug.py` | 新增 | 113 项 W5 全面测试 |
| `requirements.txt` | 修改 | +gradio |

---

## §10 W5 事后 Debug（2026-06-01 06:00）

创建 `tests/test_w5_debug.py` — 104 项测试，11 个测试段。

### 测试覆盖

| 段 | 测试项 | 覆盖内容 |
|----|--------|---------|
| 1 | 22 | Injection detection: 27 built-in + empty/whitespace/long/multiline/obfuscated/unicode/Japanese |
| 2 | 17 | File validation: 7 extensions, uppercase, no-ext, double-ext, size limits |
| 3 | 6 | EXIF stripping: create/verify/cleanup, non-image passthrough |
| 4 | 10 | RAG sanitization: marker, SYSTEM stripping, code block, multiple lines |
| 5 | 11 | Tool call validation: 6 valid + invalid + injection args + edge |
| 6 | 3 | File hashing: SHA-256, consistency, uniqueness |
| 7 | 8 | Real usage: injection blocking, multi-block recovery, tool calls, edge cases |
| 8 | 8 | Web app: import, build_ui, cost HTML, session management |
| 9 | 11 | Docker: file existence, structure, port, env vars, requirements |
| 10 | 9 | Edge cases: 10K args, emoji, ZWSP, rapid-fire, HTML in RAG |
| 11 | 4 | Pattern quality: compilation, IGNORECASE, marker integrity, extensions |

### 发现的 Bug

**Bug 18: `detect_injection` 缺少 DOTALL 标志 — 多行注入漏检**
**文件**：`agent/security.py:128`
**症状**：`re.search(pattern, text_lower, re.IGNORECASE)` — 默认模式 `.` 不匹配 `\n`。输入 `"\n\nIgnore\n\nall\n\ninstructions"` 中的 `ignore.*instructions` 无法跨行匹配，导致多行注入漏检。
**修复**：改为 `re.search(pattern, text_lower, re.IGNORECASE | re.DOTALL)`。

### 代码审查发现

**Bug 19: `strip_exif` 泄漏临时文件**
**文件**：`agent/security.py:72` — 函数创建 `filepath + ".clean" + ext` 但从不清理。
**修复**（c10b4bf）：添加 `atexit.register(_cleanup_temp_files)` + `cleanup_temp_files()` 公开函数 + EXT_TO_MIME 映射表。

**Bug 20: `security.py` 死代码**
- `import io` — 未使用 → **已删除**
- `import PIL.ExifTags` — 未使用 → **已删除**
- `ALLOWED_MIME_TYPES` — 未使用 → **替换为 EXT_TO_MIME 映射表（7 个扩展）**

**Bug 21: `strip_exif` 内存风险**
`data = list(img.getdata())` — 20MB 图像可膨胀到 100+ MB。
**缓解**（c10b4bf）：添加文档说明，临时文件注册到 atexit 清理，大图限制 20MB 上限阻止极端情况。

### 结果

113/113 全部通过。全部 4 个 bug 已修复。

---

## §11 Bug 汇总（W5 全部修复）

| # | 文件 | 严重度 | 描述 | 状态 |
|---|------|--------|------|------|
| 18 | security.py | **高** | detect_injection 缺 DOTALL→多行注入漏检 | ✅ fixed (de9a4dd) |
| 19 | security.py | 中 | strip_exif 泄漏 .clean 文件 | ✅ fixed (c10b4bf) |
| 20 | security.py | 低 | 死代码 (io, ExifTags, ALLOWED_MIME_TYPES) | ✅ fixed (c10b4bf) |
| 21 | security.py | 低 | strip_exif list() 内存 100+ MB | ✅ 缓解 (c10b4bf) |
| 22 | web/app.py | **高** | Gradio 4.44.1 schema bool bug→启动崩溃 | ✅ fixed (47b89f9) |
| 23 | prompts.py + core.py + config.py | **高** | 自我纠错泄漏 + thinking 关闭 | ✅ fixed (2631b19, 9599798) |
| 24 | web/app.py + core.py | **高** | Mermaid 图表 5 次尝试才成功渲染 | ✅ fixed (6ec7abf→9b385f5, 7 commits) |
| 25 | web/app.py + prompts.py | **高** | LaTeX 公式 + f-string `{` 解析 | ✅ fixed (6af0756) |
| 26 | __pycache__ | 中 | Python 缓存旧字节码阻塞新代码 | ✅ fixed (rm __pycache__) |
| 27 | prompts.py | 低 | circuitikz label Unicode Ω 不被 Kroki 支持 | ✅ fixed (数学模式 $\Omega$) |

**W5 全部 10 个 bug 已修复。**

## §12 未完成项

| 任务 | 说明 | 状态 |
|------|------|------|
| W5.5 学生试用 | 需要真人 A-Level 学生 | ⚠️ 等待人类介入 |
| W5.6 演示视频 | 需要录屏 | ⚠️ 等待人类介入 |
| W5.8 多科目校准运行 | 45 案例代码就绪，需 API 运行 | 📋 代码 ready |
| W5.9 QP+MS 映射 | 方案设计完成，需 VLM 批处理 2h | 📋 设计 ready |

---

## §14 LLM 处理动画展示（2026-06-01，用户需求）

在 Web UI 中添加类似 ChatGPT "Thinking..." / Claude Code "Grepping..." 的实时状态动画。

**实现**（b1208ac）：

**Agent 侧** (`core.py`)：
- `Agent.__init__` 新增 `status_callback` 参数（`fn(str)` 回调）
- 思考阶段：`status_callback("🎯 正在深度思考...")`
- 工具调用阶段：`TOOL_STATUS` 映射表，6 种工具各有专属状态
  - `📚 正在检索教材...` / `📝 正在检索真题...` / `🎯 正在分析解题套路...`
  - `💡 正在查找答题技巧...` / `📸 正在 AI 批改...` / `📋 正在查询科目信息...`

**Web 侧** (`web/app.py`)：
- 聊天框上方新增 `gr.HTML` status_display 组件
- `_build_status_html()` — 最近 5 条状态消息渲染为紧凑时间线
- `chat_fn` 通过闭包 accumulator 收集回调消息，返回三元组 (msg, history, status_html)
- 状态在 LLM 返回后一次更新（sync 模式限制，未来可改为 streaming）

---

## §13 用户反馈修复（2026-06-01）

### Bug 22: Gradio 4.44.1 + Python 3.9 schema 类型 bug

**症状**：启动 `web/app.py` 时报 `TypeError: argument of type 'bool' is not iterable`，来自 `gradio_client/utils.py:863` 的 `get_type()`。Gradio 4.44.1 在 Python 3.9 上遇到 bool schema 时崩溃。Gradio 5.x 需要 Python 3.10+。

**修复**（47b89f9）：在 `web/app.py` 顶部 monkey-patch `gradio_client.utils.get_type()`：
- 若 schema 是 bool 类型，返回 `"boolean"`
- 否则委托原始函数
- 必须在 `import gradio` 之前执行

### Bug 23: DeepSeek 自我纠错过程泄漏到学生输出

**用户反馈**：回答中出现「流入 = 5A，流出 = 3A + 4A = 7A ❌ 不对！实际上...等等，重新算」——LLM 的内部草稿纠错过程直接展示给学生，且 ASCII 电路图错误。

**根因分析**：
1. DeepSeek V4（94% 幻觉率）在长篇回答中「边写边纠正」
2. `tutor` 模型 `thinking=False`——没有独立的推理链，LLM 的思考过程直接写入 `content`
3. ASCII 艺术图是 LLM 的固有弱点——公式对但图编的

**修复**（2631b19 + 9599798），三层防护：

| 层 | 文件 | 措施 |
|----|------|------|
| System Prompt | prompts.py | 新增 `⛔ 绝对禁止` 章节：禁止输出纠错标记（不对/等等重新算）、禁止编造不确认的公式、禁止展示工具调用过程 |
| 后处理过滤 | core.py | `_sanitize_output()` — 正则检测自我修正模式 → 在回答前加 ⚠️ 警告 |
| Thinking 模式 | config.py | `tutor.thinking = True` — 启用独立推理链，推理在 `reasoning_content` 中、答案在 `content` 中，互不干扰 |
| Reasoning 连续性 | core.py | `chat()` 保存 `reasoning_content` 到 conversation history，确保多轮对话 + tool call 不触发 HTTP 400 |

### Bug 24: Mermaid.js 图表渲染失败（5 次尝试）

**用户反馈**：LLM 输出的 ASCII 电路图排版错乱，「mermaid 还是不行，不能正常显示而是显示了代码」。

**尝试 1：系统 Prompt 引导 → 失败**（6ec7abf）
在 prompt 中添加 Mermaid 示例，期望 LLM 自觉输出 `` ```mermaid `` 代码块。
结果：LLM 能生成 Mermaid 代码，但 Gradio 将其显示为源代码（Gradio 4.44.1 不含 Mermaid.js 渲染器，PR #10854 未进入此版本）。

**尝试 2：CDN 注入 + MutationObserver → 失败**（53564c5 + de9d266 + e357a65）
通过 `gr.Blocks(head=...)` 注入 Mermaid.js CDN，用 MutationObserver 扫描 DOM 转换 `<code class="language-mermaid">` → `<pre class="mermaid">`，调用 `mermaid.run()` 渲染。
失败原因：
- `head` 参数在 Gradio 4.44.1 中不可靠
- `gr.HTML(visible=False)` 注入的脚本可能执行时机不对
- MutationObserver 找不到 Gradio Chatbot 生成的 DOM 元素
- 即使脚本成功注入，浏览器 F12 Console 无报错但不渲染

**尝试 3：Kroki API 服务端渲染 → 逻辑正确但缓存导致失败**（6af0756）
改用 `_render_mermaid_blocks()` — 服务端 POST Mermaid 代码到 `https://kroki.io/mermaid/svg` → 返回 SVG → base64 编码 → 嵌入 `<img>` 标签。
问题：Python `__pycache__` 缓存了旧版 `_sanitize_output`（不含 `_render_mermaid_blocks` 调用）。清除 `__pycache__` 后函数调用恢复正常。

**尝试 4：原始 HTML `<img>` → Safari 兼容失败**（6af0756）
Kroki API 渲染成功，但 Safari 上 `<img src="data:image/svg+xml;base64,...">` 不显示。
原因：Gradio Chatbot 的 markdown 渲染器（marked.js）对原始 HTML 标签进行安全过滤，`<img>` 被 strip 掉。

**尝试 5：Markdown 图片语法 → 成功**（9b385f5）
改用标准 Markdown 图片语法：`![diagram](data:image/svg+xml;base64,...)`
结果：**所有浏览器正常显示**。Markdown 解析器原生支持此语法，不做过滤。

**最终方案**（7 个 commit）：
1. System prompt 包含 Mermaid 示例 + ASCII 禁止规则
2. `_render_mermaid_blocks()` 服务端渲染（Kroki API，~4s）
3. `_sanitize_output()` 检测并追加 ASCII 警告
4. 输出使用 Markdown `![diagram](base64SVG)` 语法

### Bug 25: LaTeX 公式不渲染

**用户反馈**：「表格里公式显示错误」。

**根因**：
1. Chatbot `latex_delimiters` 只配了 `$$`（独立公式），缺少 `$`（行内公式），表格中的 `$E=mc^2$` 不渲染
2. System prompt 的 `base = f"""...` 是 f-string，LaTeX 的 `{...}` 花括号被 Python 当成 f-string 表达式解析，导致 `SyntaxError`

**修复**（6af0756）：
- Chatbot 添加 `{"left": "$", "right": "$", "display": False}`
- `base = f"""` → `base = """`，subjects_summary 单独 f-string
- System prompt 新增 `LaTeX 数学公式规范` 章节（常用命令、表格写法、禁止 Unicode 伪公式）

### Bug 26: Python `__pycache__` 缓存旧字节码

**症状**：`_render_mermaid_blocks` 函数代码正确但完全不执行，`_sanitize_output` 输出仍含原始 Mermaid 代码块。

**根因**：修改 `_sanitize_output` 后添加 `_render_mermaid_blocks()` 调用，但项目目录下的 `__pycache__` 保存了旧版 `.pyc` 字节码。Python 加载缓存而非重新编译源文件。

**修复**：`find . -type d -name __pycache__ -exec rm -rf {} +`

**教训**：每次修改函数的调用关系后应清理缓存，或设置 `PYTHONDONTWRITEBYTECODE=1`。

---

## §15 统一图表生成系统（2026-06-01）

### 15.1 需求调查

遍历 30 种内容类型（4 科），确定图表需求：

| 科目 | 图表类型 | 推荐引擎 |
|------|---------|---------|
| 9701 Chemistry | 化学结构式、反应机理、能量循环、分子几何、光谱图 | TikZ (chemfig/pgfplots) |
| 9702 Physics | 电路图、力/体图、波形图、场线图、实验装置 | TikZ (circuitikz/pgfplots) |
| 9708 Economics | 供需曲线、AD-AS、弹性图、PPC、Phillips | TikZ (pgfplots) |
| 9709 Mathematics | 函数图、坐标几何、力学图、向量图 | TikZ (pgfplots) |
| 跨科 | 流程图、数据表、概念图 | Mermaid / Markdown |

TikZ 覆盖 ~18/30 种类型。

### 15.2 Kroki 引擎验证

| 包 | 用途 | 状态 |
|----|------|------|
| circuitikz | 电路图 | ✅ 200 OK |
| chemfig | 化学结构 | ✅ 200 OK |
| mhchem | 化学方程式 | ✅ 200 OK |
| pgfplots | 函数/经济图 | ✅ 200 OK |

踩坑：`\documentclass{standalone}` 的 `{...}` 被 Python `str.format()` 解析 → `KeyError`。修复：全部 `{` 改为 `{{` escape。

### 15.3 实现

`agent/diagrams/renderer.py` — 4 引擎统一渲染：

| 函数 | 输入 | 模板 |
|------|------|------|
| render_mermaid | Mermaid 代码 | — |
| render_tikz | TikZ 代码 | circuit/graph/force/chemfig/mechanism/general |
| render_vegalite | Vega-Lite JSON | — |
| render_graphviz | Graphviz DOT | — |
| render_all_diagrams | LLM 原始输出 | 自动检测 ```类型 → 渲染 |

System prompt 更新：5 引擎示例 + `template=` 标签 + Unicode 警告。

### 15.4 Bug 27: circuitikz Unicode 字符

Kroki LaTeX 不支持标签中的原始 Unicode（如 Ω）。必须用数学模式 `$\Omega$`。在 prompt 中已添加警告。

---

## §16 图表精度革命——预置模板系统（2026-06-01）

### 16.1 问题诊断

**用户反馈**：「经济学复合曲线图有严重位置错误，大体在模仿正确的样子但相差甚远。」

**深度研究**：
- DeTikZify (arXiv 2405.15306)：即使 360k 训练样本，LLM 生成的 TikZ 仍有 fidelity errors（交点不准、比例失调）
- CAGE (arXiv 2604.09691)：代码生成精度高（96% LEM）但视觉质量差 → 需要「code anchors + diffusion refinement」
- TeX StackExchange：用户求助「TikZ 图不匹配原图」，回答用 Metapost 重写——根本原因是 LLM 不懂数学坐标

**根本结论**：LLM 不具备生成精确数学坐标的能力。这是一个已知的学术难题。

### 16.2 解决方案

**预置模板库**——LLM 只负责选模板和填标签，不生成坐标：
```
LLM 输出: ```tikz template=econ:ad-as```
      ↓
服务端: 加载预验证 TikZ → 填默认标签 → Kroki 渲染
      ↓
结果: 精确交点、正确斜率、标准标注
```

### 16.3 模板库

| 科目 | 模板数 | 覆盖图 |
|------|--------|--------|
| 9708 Economics | 9 | 供需均衡、需求右移、负外部性(DWL)、AD-AS、AD 扩张、税收归宿、PPC、最高限价、凯恩斯 LRAS |
| 9702 Physics | 5 | 串联电路、并联电路、自由体图、波形图、斜面力分解 |

**精确性保证**：每个模板的交点都通过联立方程计算：
- `demand-supply`: D=-x+6, S=0.8x+0.5 → E=(3.06, 2.94)
- `ad-as`: AD=-0.9x+7.5, SRAS=0.5x+2 → E=(3.93, 3.96)
- `negative-externality`: DWL 三角形顶点精确计算

### 16.4 实现

| 文件 | 说明 |
|------|------|
| `agent/diagrams/templates/economics.py` | 9 个经济模板 + `render_template()` + `list_templates()` |
| `agent/diagrams/templates/physics.py` | 5 个物理模板 |
| `agent/diagrams/renderer.py` | `extract_and_render_tikz` 扩展支持 `econ:` / `phys:` 前缀 |
| `agent/tutoring/prompts.py` | 模板目录（9 econ + 5 phys）写入 system prompt |

**模板引用语法**：`` ```tikz template=econ:ad-as``` `` / `` ```tikz template=phys:series-circuit``` ``

### 16.5 未来扩展

- Chemistry 模板（能量循环、反应机理、分子几何）
- Mathematics 模板（函数图、坐标几何、向量）
- 模板参数化：LLM 可通过模板 label 替换自定义标注

---

## §16 回滚记录（2026-06-01）

**回滚到**：dcd4afe (feat(w5): unified diagram generator)

**丢弃的 commit**（13 个，保存在 backup-pre-rollback 分支）：
3455a74 auto-inject economic diagram templates
3afb4eb 4 new econ templates (subsidy, minimum-price, monopoly, monopsony)
7d31c24 SVG size reduction
381d9b1 SVG base64 not stored in conv history
2d90cf0 PNG→SVG (16K vs 58K)
e0b36d4 VLM-verified diagram fixes
6a48271 46 econ diagram tests
3b07405 remove json econ plotter back to Kroki
4afbd42 tariff template rewrite
7b27bd8 w5 log §15 diagram system
65e13c3 w5 log §15 diagram system
dcd4afe ← 当前 HEAD
...plus 5 more

**丢失的内容**：
- `agent/diagrams/templates/economics.py` (14 个预建经济模板)
- `agent/diagrams/templates/physics.py` (5 个物理模板)
- `agent/diagrams/econ_plotter.py` (matplotlib 渲染引擎)
- `tests/test_econ_diagrams.py` (46 项经济图测试)
- w5.md §13-15 (Mermaid saga, LaTeX, VLM, 模板系统)
- w5.md §11 bug 22-27
- 200+ 项 debug 测试

**回滚原因**：图表渲染系统经过 13 次迭代仍未稳定。base64 inline + Gradio 组合在规模下失败。重新从 Kroki baseline 出发，用文件 URL 替代 base64 inline。

**后续计划**：`docs/plans/diagram_system_v2.md`

**保留在 backup-pre-rollback 分支**：所有 13 个 commit 未丢失，可随时 cherry-pick。

---

## §17 图表 V2 Phase 3-4（2026-06-01）

### 17.1 Phase 3: Cambridge 标准图表风格

**commit 8cf5dcd**：
- 100 DPI 提升清晰度
- Bold labels — 坐标轴字体加粗
- Larger figure size — 增大画布防止文字重叠
- Cambridge exam style 色板：蓝(D)、红(S)、绿(MSB)……
- commit ce95399 — intersection fix plan 文档

### 17.2 Phase 4: spec_builder — LLM 描述意图，Python 计算精确坐标

**commit fa5085c**：
- 新增 `agent/diagrams/spec_builder.py`
- 模式：`{"type": "demand_supply", "elasticity": "elastic"}` → builder 计算完整 spec → plotter 渲染
- 14 种经济图类型。48 项图表测试全部通过，VLM 验证 Grade A on 2/3 类型

**commit 59ab005**：填色用 Pe/Qe 标签，不再用手动 x1/x2 坐标

**commit 76b4271**：PNG → SVG 输出
- 输出从 base64 PNG (50KB) 切换到 SVG (16KB)，体积 3x 减小
- 彻底解决中文乱码：SVG 直接嵌入文字不依赖 matplotlib 字体
- 新增 LLM 生成 SVG 的系统提示模板
- 限制每回复最多 2 张图（避免 8 张图刷屏）

## §18 KaTeX Parse Error 排查与修复（2026-06-01）

> Commits: 3d475eb, c8ec30a

### 18.1 症状

Gradio Chatbot 显示 "KaTeX parse error" 而非公式渲染。

### 18.2 根因

| # | 问题 | 示例 | KaTeX 行为 |
|---|------|------|-----------|
| 1 | LLM 用 `$` 当货币符号 | `价格 $10` | 把 `$10` 当数学块起始，无配对 `$` → 报错 |
| 2 | `&` 在 `$...$` 内部 | `$A & B$` | KaTeX 将 `&` 视为对齐符，不允许在行内公式中 |
| 3 | `\ce{}` 命令 | `$\ce{H2O}$` | KaTeX 不支持 mhchem 扩展 |

### 18.3 修复：双层防线

**① 系统提示层** (`prompts.py`)：
- 明确禁止 4 项：`$` 当货币（写"10元"）、`&` 在 `$...$` 中、`\ce{}`、不配对 `$`
- 正反面示例

**② 后处理层** (`core.py:_fix_katex`)：
- 逐字符扫描器（非正则），正确处理 `$$a = $b$ + c$$` 嵌套
- 预处理：`$10` → `10元`（匹配 `$`+数字，保留 `$10$`）
- LaTeX 块内：`&` → `\text{ and }`、`\ce{...}` → `\text{...}`
- LaTeX 块外：游离 `$` → `＄`（全角，不作分隔符）

**③ 测试**：9 项全部通过（货币、行内公式、独立公式、混合、游离、嵌套、`&`修复、`$10$`保留、千分位）

## §19 GUI 调试日志系统（2026-06-01）

### 19.1 动机

KaTeX/SVG 渲染错误发生在浏览器端，服务端无记录。每次调试需猜测 LLM 原始输出。

### 19.2 实现

**日志文件**：`data/gui_debug.log`（JSON lines，保留 500 条）

每轮记录字段：

```
session  subject  model      conv_len  svg_count  raw_len  elapsed_s
user_msg  response_len  error
katex_fixes: {currency, stray_dollar, ampersand, ce_command}
tool_calls: [{tool, args, result_len}, ...]
response: <前5000字原始输出>
```

**数据来源**（`core.py` 新增 6 个全局追踪变量）：
- `_last_raw_output` — sanitize 前原始输出
- `_last_tool_calls` — 每轮 tool call 详情
- `_last_response_time` — 响应耗时
- `_last_model` — 当前模型
- `_last_svg_count` — SVG 图表数
- `_last_conv_len` — 对话历史长度

## §20 本节代码变更

| 文件 | 说明 |
|------|------|
| `agent/tutoring/prompts.py` | LaTeX KaTeX 安全规范、禁止项、图量限制 |
| `agent/tutoring/core.py` | `_fix_katex` 扫描器 + 6 个全局追踪变量 |
| `web/app.py` | `_log_debug()` + `chat_fn` 全字段日志 |
| `.gitignore` | +data/rendered/、+gui_debug.log |
| `docs/log/w5.md` | §17-20（本节） |
| `docs/log/diagram_v2.md` | 补写 Phase 3-4 |
